# Jarvis Ultimate 0.0.9 STT / microphone input dependencies
# Install from the project root with:
#   python -m pip install -r requirements-stt.txt

# Microphone recording
sounddevice>=0.5.0
numpy>=1.26,<2.0

# Local/offline speech recognition provider
# 0.0.9b defaults Jarvis STT to device=auto so CUDA is used when the
# faster-whisper/CTranslate2 GPU runtime is available.
faster-whisper>=1.2.1
ctranslate2>=4.6.0
