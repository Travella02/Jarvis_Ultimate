# Permissions System

Jarvis should centralize risky actions through a permissions layer.

Examples:
- Opening an app may be allowed.
- Deleting files should ask.
- Sending emails should ask.
- Starting/stopping recording may ask depending on settings.
- Writing memory can be allowed or ask depending on type.
